image:
  repository: payments-service
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8081
  targetPort: 8081

env:
  DB_URL: "postgresql://user:password@db:5432/payments"
